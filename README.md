# Goji API docs

## Run

Try using docker first:

    docker-compose pull
    docker-compose build
    docker-compose up

    http://localhost:4567

Or if you have ruby installed:

    bundle install
    bundle exec middleman server

    http://localhost:4567
